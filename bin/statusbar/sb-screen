#!/usr/bin/env bash
set -euo pipefail

curr_brightness=$(cat /sys/class/backlight/*/brightness)
max_brightness=$(cat /sys/class/backlight/*/max_brightness)
brightness_per=$((100 * curr_brightness / max_brightness))

# case $BLOCK_BUTTON in
#   1)  ;;
#   3)  ;;
#   6)  ;;
# esac

echo "br${brightness_per}"
